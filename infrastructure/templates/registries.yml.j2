mirrors:
  "{{ registry_ip }}:{{ registry_port }}":
    endpoint:
      - "http://{{ registry_ip }}:{{ registry_port }}"
configs:
  "{{ registry_ip }}:{{ registry_port }}":
    tls:
      insecure_skip_verify: true
